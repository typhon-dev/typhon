# typhon-parser

Lexer and parser for the Typhon programming language.

This crate provides the lexical analysis and parsing infrastructure that converts Typhon source code into an Abstract Syntax Tree (AST).
